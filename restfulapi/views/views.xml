<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="restfulapi.list">
      <field name="name">restfulapi list</field>
      <field name="model">restfulapi.restfulapi</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="restfulapi.action_window">
      <field name="name">restfulapi window</field>
      <field name="res_model">restfulapi.restfulapi</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="restfulapi.action_server">
      <field name="name">restfulapi server</field>
      <field name="model_id" ref="model_restfulapi_restfulapi"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="restfulapi" id="restfulapi.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="restfulapi.menu_1" parent="restfulapi.menu_root"/>
    <menuitem name="Menu 2" id="restfulapi.menu_2" parent="restfulapi.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="restfulapi.menu_1_list" parent="restfulapi.menu_1"
              action="restfulapi.action_window"/>
    <menuitem name="Server to list" id="restfulapi" parent="restfulapi.menu_2"
              action="restfulapi.action_server"/>
    -->
  </data>
</odoo>